{
    "HPC": [
        "i = 0",
        "while i < 5:",
        "print(i)",
        "i -= 1",
        "`i`",
        "`i` is decremented",
        "`i` becomes negative",
        "`i` should be incremented"
    ]
}